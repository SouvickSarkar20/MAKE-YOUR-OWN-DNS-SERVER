{
  "name": "dns-server",
  "version": "1.0.0",
  "description": "we will be creating a dns server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dns-packet": "^5.6.1",
    "nodemon": "^3.1.9"
  }
}
